# deps / build output
node_modules/
.next/
out/
.wrangler/

# NOTE: .env.local is committed ON PURPOSE for this app.
# It contains only public values (Supabase URL, publishable key, Turnstile
# SITE key). Real secrets (Telnyx API key, Turnstile SECRET, service role)
# live only in Supabase Edge Function secrets, never in this repo.
