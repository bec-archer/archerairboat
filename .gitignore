.DS_Store
node_modules/
dist/
.astro/
.env
.env.*
!.env.example

# Photoshop sources are too big for GitHub (100MB limit) — keep local only
*.psd

# Cowork VM cannot delete files; stale files get parked here — safe to trash
_to_delete/
