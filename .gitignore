.DS_Store
node_modules/
dist/
.astro/
.env
.env.*
!.env.example
# app/.env.local is committed on purpose: public values only (see app/.gitignore)
!app/.env.local

# Photoshop sources are too big for GitHub (100MB limit) — keep local only
*.psd

# Cowork VM cannot delete files; stale files get parked here — safe to trash
_to_delete/
